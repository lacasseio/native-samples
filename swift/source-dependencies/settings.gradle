plugins {
    id 'dev.nokee.sample'
}

sample {
    copySource {
        appProject(".").buildRoot()
        appProject(".").fromTemplate("swift-app")
    }
}

rootProject.name = 'app'

sourceControl {
    gitRepository("https://github.com/gradle/native-samples-swift-library.git") {
        producesModule("org.gradle.swift-samples:utilities")
    }
}
